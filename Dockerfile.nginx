# Solo sirve el artefacto ya compilado (build/web).
# Compilar antes en el host: flutter build web --release --base-href /app_mesaayuda/
FROM nginx:1.27-alpine
COPY deploy/nginx.conf /etc/nginx/conf.d/default.conf
COPY build/web /usr/share/nginx/html
EXPOSE 80
