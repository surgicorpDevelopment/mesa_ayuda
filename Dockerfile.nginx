# Sirve el artefacto copiado a deploy/webdist (robocopy, sin symlinks).
# Compilar antes: flutter build web --release --base-href /app_mesaayuda/
FROM nginx:1.27-alpine
COPY deploy/nginx.conf /etc/nginx/conf.d/default.conf
COPY deploy/webdist /usr/share/nginx/html
EXPOSE 80
